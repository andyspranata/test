name: learn
on: 
 push:
    branches:
      - production
jobs:
  testrun:
    runs-on: cayalinux
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: '14'
      - run: npm install -g bats
      - run: bats -v